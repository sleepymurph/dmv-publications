set xlabel "Number of files (log scale)"
set logscale x 10
set format x "%1.0s%c"

set ylabel "Ratio of total storage used to file size"
set format y "%.0fx"
set yrange [0:20]

set grid
set key top right

meantable="< ./meantable.py --columns filecount totalbytes c2_size --files exp--increasing-number-of-files--v02--w-cpu"

plot \
    meantable."/git-*" using 1:($4/$2):($5/$2) with errorlines title "git (two commits)", \
    meantable."/hg-*" using 1:($4/$2):($5/$2) with errorlines title "hg (two commits)", \
    meantable."/bup-*" using 1:($4/$2):($5/$2) with errorlines title "bup (two commits)", \
